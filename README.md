# L&T Realty Sector 86

npm install
npm run dev

Optional: set VITE_ENQUIRY_ENDPOINT in .env to POST enquiry JSON to your backend.
Images are Unsplash placeholders (src/data.js). Replace with official renders.
